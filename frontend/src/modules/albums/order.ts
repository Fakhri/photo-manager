// Intra-album and grid order helpers (skeleton)

import type { AlbumId, PhotoId } from './api'

export function setAlbumOrder(_albumIds: AlbumId[]): void {
  // TODO: persist order via storage
}

export function setIntraAlbumOrder(_albumId: AlbumId, _photoIds: PhotoId[]): void {
  // TODO: persist order via storage
}
