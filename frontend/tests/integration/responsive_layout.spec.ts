import { describe, it, expect } from '@playwright/test'

describe('Responsive layout', () => {
  it('adapts grid/tiles to desktop and mobile breakpoints without layout breakage', async () => {
    expect(true).toBe(true)
  })
})
