import { describe, it, expect } from 'vitest'
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import type { SearchApi } from '../../../src/modules/search/api'

describe('Search API contract', () => {
  it('should filter by date range, tags, and captions text', () => {
    expect(true).toBe(true)
  })

  it('should limit results to an album when albumId is provided', () => {
    expect(true).toBe(true)
  })
})
